opacity-rule = [
  "90:class_g = 'alacritty' && focused"
];

blur:
{
  method = "dual_kawase";
  strength = 2;
};

backend = "glx";
glx-no-stencil = true;
glx-copy-from-front = false;
use-damage = true
glx-no-rebind-pixmap = true;


